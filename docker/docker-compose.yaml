version: '3.0'
services:
    php:
        build: .
        ports:
            - "80:80"
        volumes:
            - ./../.:/var/www/html/desafio
        networks:
            - backend
networks:
    backend:
